# Apify API token — get yours at https://console.apify.com/settings/integrations
# Copy this file to .env and fill in your token before building.
# The token is injected at BUILD TIME by esbuild; re-run `npm run build` (or
# keep `npm run dev` running) after changing this value.
APIFY_TOKEN=your_apify_api_token_here
